# 🏗️ Arquitectura del Proyecto

## Flujo de Datos

```
┌─────────────────────────────────────────────────────────────┐
│                      app/page.tsx                           │
│                    (Página Principal)                       │
└─────────────────┬───────────────────────────────────────────┘
                  │
        ┌─────────┼─────────┬──────────┬─────────┐
        ▼         ▼         ▼          ▼         ▼
    ┌───────┐ ┌─────────┐ ┌─────────┐ ┌──────┐ ┌─────────┐
    │Navbar │ │HeroSec. │ │AwardsSec│ │Social │ │Footer   │
    └───────┘ └─────────┘ └────┬────┘ └──────┘ └─────────┘
                                │
                    ┌───────────┴────────────┐
                    ▼                        ▼
                ┌────────┐            ┌────────┐
                │AwardCard 1│          │AwardCard 2│
                └────────┘            └────────┘
```

## Estructura de Carpetas Detallada

```
📦 app/
├── 📂 components/           # Componentes reutilizables
│   ├── Navbar.tsx
│   ├── HeroSection.tsx
│   ├── AwardsSection.tsx
│   ├── AwardCard.tsx
│   ├── SocialLinks.tsx
│   ├── SocialIcons.tsx
│   └── index.ts             # Exporta todos los componentes
│
├── 📂 constants/            # Datos estáticos
│   └── raffleData.ts        # Datos de la rifa
│
├── 📂 types/                # Tipos TypeScript
│   └── index.ts
│
├── 📂 config/               # Configuración
│   └── index.ts
│
├── 📂 hooks/                # Custom hooks
│   └── index.ts
│
├── 📂 utils/                # Funciones auxiliares
│   └── index.ts
│
├── layout.tsx               # Layout principal (HTML)
├── page.tsx                 # Página home
└── globals.css              # Estilos globales
```

## Componentes Jerárquicos

```
                      ┌─────────────────────────────┐
                      │      RootLayout             │
                      │    (layout.tsx)             │
                      └──────────────┬──────────────┘
                                     │
                      ┌──────────────▼──────────────┐
                      │      Page Component         │
                      │     (page.tsx)              │
                      └──────────────┬──────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        │                            │                            │
        ▼                            ▼                            ▼
    ┌────────┐                  ┌───────────┐            ┌────────────────┐
    │ Navbar │                  │HeroSection│            │AwardsSection   │
    │        │                  │           │            │                │
    │ Props: │                  │ Props:    │            │ Props:         │
    │ · onClick │                │ · title   │            │ · awards       │
    │ · ...  │                  │ · progress│            │ · onSelect     │
    └────────┘                  │ · ...     │            └────────┬───────┘
                                └───────────┘                     │
                                                    ┌────────┬────┴─────┬──────┐
                                                    ▼        ▼          ▼      ▼
                                                ┌──────┐┌──────┐┌──────┐┌──────┐
                                                │Award ││Award ││Award ││Award │
                                                │Card1 ││Card2 ││Card3 ││Card4 │
                                                └──────┘└──────┘└──────┘└──────┘

                                                    (AwardCard.tsx)
```

## Flujo de Props

```
┌─────────────────────────────────────────────┐
│  raffleData (constants/raffleData.ts)      │
│  ├── title                                  │
│  ├── description                            │
│  ├── progress                               │
│  ├── awards[]                               │
│  └── ...                                    │
└────────────┬────────────────────────────────┘
             │
    ┌────────┴──────────┬──────────────┬────────────┐
    ▼                   ▼              ▼            ▼
  Navbar          HeroSection     AwardsSection  SocialLinks
    │                   │              │            │
    │                   └─► props      │            │
    │                                  │            │
    └──────────────────► onLoginClick ◄┘            │
                                       │            │
                            onAwardSelect()         │
                                       │            │
                        ┌──────────────┴───────────►│
                        │                           │
                   AwardCard[]              buildWhatsAppUrl()
```

## Flujo de Eventos

```
Usuario
   │
   ├─ Click "Iniciar sesión" ──► Navbar ──► onLoginClick() ──► page.tsx
   │
   ├─ Click "Comprar" ──► AwardCard ──► onBuy() ──► manejo en page.tsx
   │
   ├─ Click "¿Qué día juega?" ──► HeroSection ──► onCheckDay() ──► page.tsx
   │
   └─ Click Instagram/WhatsApp ──► SocialLinks ──► link externo
```

## Separación de Responsabilidades

| Capa | Responsabilidad | Ubicación |
|------|-----------------|-----------|
| **Presentación** | Mostrar UI | `components/` |
| **Datos** | Almacenar info | `constants/` |
| **Tipos** | Definir tipos | `types/` |
| **Config** | Configuración global | `config/` |
| **Lógica** | Custom hooks | `hooks/` |
| **Utilidades** | Funciones auxiliares | `utils/` |
| **Orquestación** | Ensamblar todo | `page.tsx` |

## ✨ Beneficios de Esta Estructura

✅ **Modular**: Cada componente es independiente  
✅ **Escalable**: Fácil agregar nuevos componentes  
✅ **Mantenible**: Código organizado y limpio  
✅ **Testeable**: Componentes aislados y tipados  
✅ **Profesional**: Sigue best practices de React/Next.js  
✅ **Reutilizable**: Componentes sin lógica de negocio específica  

---

Esta arquitectura permite que el proyecto crezca sin volverse caótico. 🚀
